using Godot;
using System;

/*public class Exit_Game_Button_Pushed : Node2D // Only godot node can be added to the tree so we have to extend one
{
	public override void _Input(InputEvent inputEvent)
	{
		if (inputEvent.IsActionPressed("ExitGamePushed"))
		{
			Window.Close;
		}
	}
}
*/
